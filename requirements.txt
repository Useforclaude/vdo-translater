# Core dependencies
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional features
watchdog>=3.0.0  # For auto-reload

# AI/ML dependencies
openai>=1.0.0
openai-whisper>=20231117

# Optional: Redis caching (comment out if not using)
# redis>=5.0.0

# Optional: Claude API (alternative to OpenAI)
# anthropic>=0.18.0
